# Local-only: the `modal` CLI used to deploy/serve the inference app.
# The container's own setup (Ollama install, fastapi) lives in inference.py via
# the image definition — that is the single source of truth. Don't duplicate here.
modal>=0.73.0
